- defaults:
    link:
        relink: true
        create: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim:
    ~/.vimrc:
    ~/.ideavimrc:   
    ~/.bashrc:

- shell:
      - [git submodule update --init --recursive, Installing submodules]


# vim:ts=4:shiftwidth=4
